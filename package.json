{
  "name": "iptp",
  "version": "0.1.1",
  "author": "DX Heroes",
  "license": "MIT",
  "scripts": {
    "be:install": "cd ./be && yarn install --prod=false && yarn build",
    "fe:install": "cd ./fe && yarn install --prod=false && yarn build",
    "install": "yarn be:install && yarn fe:install",
    "start": "cd ./be && yarn start:prod"
  },
  "engines": {
    "node": ">=12.18.1"
  }
}
